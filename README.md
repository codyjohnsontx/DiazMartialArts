# Review screenshots for PR #31

Header captures embedded in the body of
https://github.com/codyjohnsontx/DiazMartialArts/pull/31

- `header-desktop.png` - 1440px viewport
- `header-mobile.png` - 390px viewport, menu open

Both taken against `next start` on the production build with
`NEXT_PUBLIC_ONDEMAND_URL` unset, which is what production runs today.

This branch carries no source code and exists only so the images have a URL
GitHub can render. Delete it once PR #31 is reviewed.
